Fix peer review access gate, report count wording, and add notification management

- PeerReview: replace silent redirect with an explicit "You don't have
  access to this page" gate for signed-out/non-publisher users,
  mirroring the existing Publish a Report pattern
- Reports: drop ", 0 reports so far" when the report count is zero
- NavBar: show the user's profile picture (with initials fallback)
  instead of their name, via a new shared Avatar component
- NavBar: add a glowing dot on the avatar for unread notifications,
  polling unread-count and dismissing on profile click (reappears if
  a new notification arrives after dismissal)
- Profile: add per-notification selection with "select all", plus
  bulk "Mark as read" and "Delete" actions
- Backend: add POST /api/notifications/read and
  POST /api/notifications/delete for bulk, user-scoped notification
  updates, backed by new mark_notifications_read() and
  delete_notifications() model functions