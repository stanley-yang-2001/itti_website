Turn "Read" into a real /reports/:id page instead of a same-page
modal (also fixed in Peer Review); align Reports header with
Country Profiles; add back-to-Reports links

client/src/pages/ReportView.jsx                               [A]
    New page at /reports/:id - PDF iframe / DOCX-via-mammoth
    viewer, carried over from the deleted ReportViewerModal but
    as a real routed page: shareable/bookmarkable URL, works with
    the browser back button.

client/src/components/ReportViewerModal.jsx                   [D]
    Removed - replaced by ReportView.jsx everywhere it was used.

client/src/App.jsx                                             [M]
    Registered /reports/:id.

client/src/pages/Reports.jsx                                   [M]
    "Read" now navigates to /reports/:id instead of opening the
    modal.

client/src/pages/PeerReview.jsx                                [M]
client/src/pages/PeerReviewMine.jsx                             [M]
    Same modal-to-page fix applied here too - reviewers read
    not-yet-published reports through the same /reports/:id page
    (already permitted server-side by _can_view_report). Added
    "← Back to Reports" at the top-left of every state (logged-out
    gate, non-publisher gate, main view).

client/src/pages/ReportPublish.jsx                              [M]
    Added "← Back to Reports" at the top-left of every state.

client/src/pages/Profile.jsx                                   [M]
    Fixed a pre-existing bug found along the way: the Favorites
    tab rendered ReportCard without an onRead prop, which would
    throw on click. Now navigates to /reports/:id; removed the
    now-redundant separate "View in Reports" link.

client/src/components/ReportCard.jsx                            [M]
    Updated onRead doc comment (no longer references a modal).

client/src/styles/App.css                                       [M]
    New shared .page-back-link utility class, used by all four
    pages above rather than each defining its own.

client/src/styles/Reports.css                                   [M]
    Reports' header padding/heading size matched to Country
    Profiles' (.country-profiles-page/.country-profiles-heading)
    exactly; kept the wider 1080px content max-width for the
    report grid. Removed the old modal-overlay CSS, replaced with
    ReportView's page-level styles (reused the existing PDF/DOCX
    rendering rules unchanged).

Verified: full vite build compiles cleanly; all four pages'
back-links and CSS confirmed present in the built bundles.