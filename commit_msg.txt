Add interactive About page, site branding, and fix broken Observatory imports

About page (client/src/pages/About.jsx, styles/About.css)
- Replace the ITTI placeholder with a full About page built from the
  institute's About PDF: overview, parent-org relationship, mission,
  trauma observatories, analytic frameworks (GTBI/ETTI/ITCS), advisory
  services, who we serve, ethics, why it matters, partnership/funding,
  and contact.
- Sticky in-page nav with scrollspy (IntersectionObserver) that jumps to
  and highlights the active section; collapses to a horizontal pill bar
  on mobile.
- Expandable accordion panels for the three analytic frameworks and the
  "Who We Serve" categories.
- Adds a dedicated "A Division of Outlets for Hope, Inc." section linking
  to https://ofhusa.org/#itti.

Branding
- Rename "About ITTI" -> "About" in the navbar, footer, and homepage
  explore cards.
- Add the ITTI seal logo to the top-left navbar (now a home link) and to
  the footer; add a favicon.
- Footer now credits "A division of Outlets for Hope, Inc." with a link.
- Re-cut the supplied logo PNG with a proper circular alpha mask — the
  original had a checkerboard baked into its pixels rather than real
  transparency (public/itti-logo.png, public/favicon.png).

Typography
- Switch headers and page titles (.display, h1/h2/h3, navbar-page-title,
  country-name) to Times New Roman across the site; body copy stays on
  Inter.

Fixes (pre-existing, discovered while verifying the build)
- Correct Observatory.jsx gadgets, they are now placed in the correct directory
- Add missing recharts dependency required by the Observatory page.
- Fix case-sensitive import mismatches (observatoryData -> ObservatoryData)
  that only broke on case-sensitive filesystems.