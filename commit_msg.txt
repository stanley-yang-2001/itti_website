Add 10-section navigation, author/date panel, and inline PDF/DOCX
reader to Reports

Backend:
- models/report.py: new Report.category column (REPORT_CATEGORIES
  constant, one of the 10 fixed sections), included in to_public_dict().
- validation.py / app.py: category is now required and validated on
  POST /api/reports, same pattern as title/description.
- migrations: 6ff385893016_add_category_to_reports.py was already
  correct and untouched. Added 8ec7a9f38009_add_index_on_reports_category.py
  to close a pre-existing gap - the model declared index=True on
  category but no migration ever created it, so a database that only
  ever ran through `alembic upgrade head` was missing the index.
- globe_data.py / country_profiles_upload.py: added restore_upload()
  and list_uploads()/restore_docx() respectively - the admin Control
  panel's restore routes in app.py already called these, but the
  functions didn't exist yet on this branch (would have thrown
  AttributeError). Also cleaned up stale docstring references to
  country_profiles.extract.py, the dotted filename this branch had
  already renamed to country_profiles_extract.py in a separate fix.

Frontend:
- ReportCard.jsx: now shows the author's name and both dates -
  Updated is the visually prominent one, Published is secondary.
- ReportViewerModal.jsx (new): inline PDF (<iframe>) / DOCX (mammoth,
  lazy-loaded) reader instead of forcing a download.
- ReportUploadForm.jsx: added the Section <select>.
- constants/reportCategories.js (new): single source of truth for the
  10 category labels, mirroring REPORT_CATEGORIES in models/report.py.
- Reports.jsx / Reports.css: merged the section-nav/pager/reader-modal
  feature with this branch's own ?highlight=<id> deep-link scroll
  feature - a highlighted report can be in any section, so
  activeCategory now jumps to wherever it actually lives before the
  scroll-into-view runs.
- package.json/package-lock.json: added mammoth.

Rebased onto database @ ecb9c9f (Scope cascading reveal to the home
page only; add globe hover tooltip). Verified: single alembic head
with no schema drift beyond pre-existing SQLite reflection noise; full
py_compile pass; a 10-point end-to-end smoke test covering category
validation/creation together with the Fellows/Control-panel/restore-
history/update-profile features already on this branch, all in one
process; npm install + vite build clean; no conflict markers anywhere
in the tree.