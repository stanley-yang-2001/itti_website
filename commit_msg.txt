Starting from "what's missing for a fully functional site," here's everything landed, in commit order on the database branch:

f605ead — 404 page + email verification (signup no longer auto-logs-in; login blocks unverified accounts)
8bc9b88 — CSRF protection (double-submit cookie pattern, every mutating request across the whole client migrated)
9da7b95 — Terms-of-Service content added to Privacy Policy (marked draft, needs legal review)
0794458 — Database backup documentation + fixed a real gap (production DB had zero backup capability due to a missing cluster_name)
8acd986 — Rate-limiting review (tightened login, closed 5 unrated auth-route gaps)
(sitewide search commit) — navbar search, full results page, backend search endpoint
eae3c6b — automated test suites for everything above