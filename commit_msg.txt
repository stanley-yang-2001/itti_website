Rebase home-page globe/panel work onto latest database branch

database moved forward again (49c6a8d, "Enlarge navbar logo and
institute name") and, importantly, that commit also properly fixed
Footer.jsx and Login.jsx, which a prior commit (aec6f89) had corrupted
with the wrong file's content. My earlier stopgap restore of those two
files is no longer needed - upstream's real fix (with the larger logo/
institute name) is better and is left untouched here entirely.

Reapplies the home-page changes (globe collapse fix + ETTI/GTBI color
coding, SidePanel data-pending bug fix, three-part restructure with
embedded panel and year dropdowns) on top of this new tip:
- Home.jsx, SidePanel.jsx, and countryData.js: full change set
  reapplied as-is, no conflicts.
- App.css: spliced my Main/Globe + Side Panel sections into the
  current file rather than overwriting it wholesale, since 49c6a8d
  changed the Navbar section (larger .navbar-logo, restructured
  .navbar-title, removed .navbar-page-title) right next to where my
  changes live. Verified line-for-line that the Navbar/Placeholder/
  Explore/Footer sections are byte-identical to origin after the
  splice - only the Main/Globe/Side Panel region differs.

Verified with `vite build` and a live dev-server run (backend +
Vite proxy) that everything compiles and works end-to-end alongside
the current NavBar/Footer.