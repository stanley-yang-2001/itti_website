Add Certifications and Fellowship pages, fix Login/SignUp legibility,
and polish sitewide navigation, typography, and footer

Certifications page
- Replace the placeholder with a fully interactive page covering all
  11 ITTI credentials, extracted from ITTI_CERTIFICATIONS.docx
  (client/src/data/certifications.js): program overview, details,
  who-should-enroll/apply, four-week curriculum, outcomes,
  requirements, credential package, and the official scope notice.
- Category filter pills, live search, expandable cards, a linked
  Level 1 -> Level 2 pathway (CGTBA(tm) -> GTBSF(tm)) with jump-to
  buttons, and a sortable "compare at a glance" table.
- 11 certification banner/flyer images, resized and compressed
  (~19MB -> ~1.2MB total) and lazy-loaded — only fetched when a card
  is expanded.

Fellowship page (renamed from Fellows)
- "Fellows" renamed to "Fellowship" in the navbar and the homepage
  explore card; pages/Fellows.jsx, styles/Fellows.css, and
  data/fellows.js renamed to Fellowship.jsx / Fellowship.css /
  fellowship.js, including the component's default export.
- Two sections built from FITTI_Executive_Deck.pdf: About the
  Fellowship (Why FITTI, the four pathway levels, Executive Value,
  Fellows Gain, who should apply, the closing Invitation) and Our
  Fellows, a roster grid (photo-or-initials, name, level badge, bio)
  with level-filter pills, backed by an intentionally empty roster
  array with a documented data shape and a "coming soon" empty state.

Login / SignUp
- Fix an invisible-heading bug: .login-card h1 / .signup-card h1 had
  no explicit color and inherited the site's near-white body text on
  a white card. Set explicit high-contrast colors throughout both
  forms and apply Times New Roman across all text. ForgotPassword and
  ResetPassword inherit the fix since they reuse Login.css.

Sitewide navigation and typography
- New ScrollToTop component, mounted in App.jsx, resets scroll
  position to the top of the page on every route change (via
  document.body, this site's actual scroll container).
- Extend the sitewide serif rule to h4-h6 for full heading coverage.
- Docs page rebuilt to match the About page's format: hero, sticky
  scrollspy sidebar, and panel sections, reusing About.css for the
  shared shell and trimming Docs.css to its content-specific styles
  (methodology cards, formula list, sources table).
- Footer cleaned up: removed every link duplicated in the navbar
  (About, Observatory, Reports, Country Profiles, Fellowship,
  Certifications, Contact), leaving only Docs.