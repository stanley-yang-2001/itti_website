Redesign Profile layout, add favorite chart/report links, align page widths

- Profile.jsx: fix broken avatar fallback, rework header spacing,
  left-align sidebar nav, standardize page container to 1080px
- Saved-chart cards link to /observatory?chart=<id>; Observatory
  restores the saved panels/indicator and shows a status banner
- Favorited report cards link to /reports?highlight=<id>, which
  scrolls to and outlines that card in the Reports list
- app.py: add GET /api/observatory/saved-charts/<id>
- api.js: add fetchSavedObservatoryChart(chartId)
- About.css/Observatory.css: align page container max-width to 1080px