Restore Country Profiles narrative-data feature (regressed in 86e7c43)

The "reconcile bright-theme/navbar/About fixes" commit merged from a
stale branch and silently reverted this feature along with the theme
change. Restoring it on top of the new bright theme rather than
undoing the theme work:

- server/data/country_profiles.json was committed as an empty (0-byte)
  file - restored the real 164-country dataset.
- GET /api/country-profiles and GET /api/country-profiles/<code> had
  been dropped from app.py entirely - re-added, matching the existing
  /api/countries route pattern.
- server/data_scripts/country_profiles_extract.py and its two source
  .docx files were never committed in the first place - added now.
- CountryProfiles.jsx had lost the fetch-and-merge logic that pulls in
  narrative profiles alongside GTBI/ETTI dashboard data, and had
  picked up a stray "Download Profile" link pointing at .docx files
  that don't exist anywhere on the site (a leftover from the Option-1
  approach we didn't take) - removed the dead link, restored the
  narrative overview/dashboard-note rendering.
- CountryProfiles.css and Unavailable.css had reverted to old
  versions whose class names no longer matched their current JSX
  (both pages would render essentially unstyled) - rewritten to match
  the actual markup, now using the new bright-theme CSS variables
  (--bg-deep, --text-hi/mid/low, --cyan, --line) instead of the
  hardcoded dark-theme hex values they previously had, so they render
  correctly under the site's current color scheme.

Verified: clean npm run build, plus a live backend check confirming
/api/country-profiles returns all 164 profiles and /api/countries
still returns all 249 countries.

Note: this same stale-branch merge also reverted the donations flow
(embedded Payment Element + admin view, back to the old Checkout
redirect) and left Reports.css with the same classname-mismatch
pattern fixed here for Country Profiles. Neither is addressed in this
commit - out of scope for this pass, flagged separately.