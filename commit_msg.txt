Merge remainder country-profile doc (85 new entries), fix About mobile
clipping and search contrast, add country-profile deep link, restore
Leadership section, add Facebook link, update Blessing/Saeed/Stanley
bios

- Merged the uploaded "remainder" docx (85 entries: 34 sovereign/
  partially-recognized states + Republic of the Congo + 50 territories/
  dependencies) into the canonical country-profile survey source,
  taking coverage from 164 -> 249 of 249 total countries in
  country_data.json (everything except Kosovo, which has no ISO
  numeric code to key it by - a pre-existing, expected gap, not new).
  - New: server/data_scripts/merge_remainder_profiles.py - merges a
    remainder docx into the existing canonical survey file (refuses to
    merge on any name collision), then re-runs the normal extract step.
    Reusable for any future gap-filling doc, not just this one.
  - country_profiles.extract.py: added a NAME_ALIASES entry for
    "Congo (Republic of the Congo)" (the remainder doc's heading text)
    -> "Republic of the Congo" (country_data.json's name for code 178),
    the one name in the new 85 that didn't resolve automatically.
  - Fixed a pre-existing bug encountered while regenerating: the
    dashboard source docx had a stray " (1)" in its filename (an
    accidental duplicate-download artifact), which doesn't match the
    exact filename the extract script looks for - renamed back to the
    canonical name. Unrelated to this doc merge, but the regenerate
    step doesn't run without it.
  - Regenerated server/data/country_profiles.json (249 entries, up
    from 164) and the canonical
    country_profile_section_of_our_international_observatory_website.docx
    (previous version backed up under country_profiles_source/old/,
    same rotation pattern the admin upload flow already uses).
- About.css: fix section content getting hidden behind the sticky
  mobile "on this page" index bar. Its scroll-margin-top only
  accounted for the main navbar's height, not the second sticky pill
  bar stacked below it, so jumping to a section on mobile landed with
  the heading/intro hidden behind the bar. Checked every other page's
  CSS for the same sticky-sub-nav-on-mobile pattern - Profile.css's
  sidebar already avoids it correctly (goes position:static on
  mobile); no other page has this issue.
- App.css: fix globe search input text blending into its own
  background (near-black text left over from the old dark theme, on
  an input background that stayed dark under the new light theme).
- SidePanel.jsx / App.css: add a second link next to "Check out our
  observatory" that deep-links to that country's Country Profiles
  entry.
- CountryProfiles.jsx: read a new `?code=` query param, auto-expand
  and scroll to that country's row on load, to support the link above.
- Fellowship.jsx: origin's own rewrite for the new database-backed
  Fellows system (admin Control panel, /api/fellows) had dropped the
  Leadership section entirely. Restored it as its own static section
  (data still lives in fellowship.js's LEADERSHIP export) next to the
  new dynamic, API-fetched Fellows grid - leadership roles don't fit
  the AFITTI/FITTI/SFITTI/DFITTI levels the Fellows table is built
  around, so there's no home for them in the new admin system.
- seed_new_fellows.py (new, in server/): the old FELLOWS array in
  fellowship.js (Blessing, Hannah, Stanley) is dead code now -
  Fellowship.jsx fetches live from /api/fellows instead, which starts
  empty. This script inserts those three as real `fellows` table rows,
  the same way the admin upload API would. Run once from server/:
  `python seed_new_fellows.py`. Idempotent (skips names that already
  exist).
- Footer.jsx: add a Facebook link.
- Moved client/public/fellows/ -> client/public/images/fellows/
  (deployment rebuild/dist cleanup steps intentionally left to you).
- Bio updates (fellowship.js; Blessing/Stanley mirrored into
  seed_new_fellows.py where duplicated):
  - Blessing Ojisor: credit her as First-Class-Honors best-graduating
    student in addition to first female SUG president, University of
    Calabar's 49th year.
  - Dr. Saeed Siddique: now credits Harvard-Medical-affiliated
    hospitals alongside the VA Boston Healthcare System.
  - Stanley Yang: added a third paragraph conveying ambition and
    openness to new opportunities/challenges ahead.