Observatory: NTO map + refs, GTBI expanded variables, dynamic country
colors, Nigeria chart reorg, user guide

- Add Nigeria geographic stressor severity map (Blessing Alims,
  published 2026-07-24) to the NTO tab, with caption + APA reference
  list on the Docs page (docs#nto-map), integrated into the sticky
  sidebar-nav Docs layout with deep-link support (#user-guide,
  #nto-map).
- countryColors.js: replace the fixed 12-color hash palette (which
  guaranteed collisions past 12 distinct countries) with dynamic
  golden-angle hue assignment - no hardcoded palette, no upper bound,
  same call signature everywhere it's used.
- Expand GTBI's chartable variables: gtbi_extract.py now aggregates
  per-exposure-type YLL/YLD/prevalence (Armed Conflict, Political
  Repression, Communal Violence, Terrorism, Forced Displacement,
  Disaster) alongside the existing composite figures, mirroring ETTI's
  domain/sub-variable structure. Added a matching "stacked GTBI
  exposure-type breakdown" chart type to the chart builder.
- NigeriaObservatory: add Burden Rate trend, YLL-vs-YLD trend, ETTI
  domain breakdown, and GTBI exposure-type breakdown charts; organize
  all charts into Trends / Comparisons / Breakdowns tabs.
- Add a Docs "How to use the Observatory" user guide section, linked
  from the Observatory page.

Rebased onto 49c6a8d (navbar/logo sizing on top of the Certifications/
Fellowship/nav-polish commit); no conflicts with either, and this
commit's Footer/Login fix means the full build now passes clean.