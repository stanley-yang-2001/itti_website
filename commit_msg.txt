Restructure Reports page into 3 sections; two-page peer review with
accumulating disapprovals

Reports page:
- Split into Browse / Publish / Peer Review sections with cascading
  reveal, matching the home page
- Report browsing is now a toggle instead of always-rendered
- Upload button replaced by a link to a new standalone /reports/publish
  page, which self-guards on auth + publisher access
- Reveal.jsx's cascade animation extended from home-only to the
  reports and peer review pages

Peer review:
- Split /peer-review into two pages: the shared "Awaiting Review"
  queue (/peer-review) and a personal "My Submissions" page
  (/peer-review/mine), both self-guarded like /reports/publish instead
  of redirecting via ProtectedRoute
- Server: rejects now accumulate like approvals instead of an instant
  single-reject transition; REQUIRED_REJECTIONS = 2 distinct rejects
  (or 1 admin reject) pulls a report from every queue for good
  (review_status = rejected), same instant-decision treatment admins
  already got on approve
- Added TYPE_REPORT_REJECTED notification, routed to the uploader's
  personal submissions page
- Extracted shared PeerReviewPanel (review controls + comment history)
  and PublisherAccessGate (login/access-level messaging) components
- Profile.jsx: notification click-through and Publications "see why"
  links now point at /peer-review/mine; added a "Not Approved" badge
  for the new rejected status
