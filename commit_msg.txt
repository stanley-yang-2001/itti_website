Move frontend build off backend, fix profile settings 404, fix ETTI
naming, add URL fragment identifiers, rewrite README

Low-memory build (Option A - GitHub Actions):
- Add .github/workflows/deploy-frontend.yml: builds the Vite bundle on
  GitHub's free runners and syncs dist/ straight to a DigitalOcean
  Spaces bucket fronted by their CDN, purging the CDN cache after each
  deploy
- app.yaml: remove the static_sites block entirely - App Platform no
  longer builds the frontend at all
- docs/DEPLOYMENT.md: document the one-time Spaces/CDN/secrets setup
  this depends on, and update the deploy-steps section to match

Fix Settings username 404:
- server/app.py: add the missing POST /api/auth/update-profile route.
  AuthContext.jsx's updateAccount() called this endpoint already, but
  it had never actually been implemented server-side, so every rename
  attempt hit Flask's default 404. Supports independent name and
  current_password/new_password updates, matching what SettingsPanel
  already sends

Fix ETTI naming inconsistency:
- Corrected two wrong expansions found site-wide: 'Election Trauma &
  Trust Index' (ControlPanel.jsx, PublishGlobeData.jsx) and 'Election
  Trauma and Tension Index' (certifications.js x2, PrivacyPolicy.jsx)
  -> 'Election Trauma Temperature Index' throughout

URL fragment identifiers for every page:
- New client/src/hooks/useHashScroll.js: shared hook that scrolls to
  the element matching the current URL hash, retrying across a few
  frames for sections whose content loads asynchronously
- Wired into Home, Fellowship, Certifications, CountryProfiles,
  Contact (added per-contact-card ids); backfilled About.jsx's
  hash-on-load effect, which Docs/PrivacyPolicy already had but About
  was missing
- Observatory.jsx: #international/#nigeria now select the matching
  main tab on load, and clicking a tab updates the hash
- Profile.jsx: #profile/#favorites/#publications/#settings now select
  the matching sidebar tab on load, and clicking a tab updates the hash

README rewrite:
- Full rewrite reflecting the site as it actually is today (was still
  describing the original placeholder-pages version). Introduces ITTI,
  defines GTBI/ETTI/Observatory/NTO vocabulary, and links every page
  plus every hash-addressable section within it, including Fellowship's
  Leadership section and Country Profiles' ?code= deep-link (a query
  param, not a hash, since it also drives which row expands).
  Deliberately excludes implementation-level detail (role-promotion
  mechanics, decorators, CLI scripts) per docs/ACCESS_LEVELS.md - only
  user-facing concepts are described